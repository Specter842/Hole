# Core. Everything except optional dispatch channels works with just these.
# Gemini is the default model provider -- free tier, rate-limited.
google-genai>=1.0
requests>=2.31

# Only needed if you set provider = "anthropic" in config.toml. Left in because
# the code path is maintained and tested; drop it if you never use Claude.
anthropic>=0.40

# Reading resumes and other material out of PDFs. .docx and plain text need
# nothing extra -- .docx is unzipped and parsed with the standard library.
pypdf>=4.0

# `jobsearch mcp` -- run the pipeline as an MCP server for Claude Code /
# Claude Desktop. Not needed for the CLI or the web UI, so it's optional
# rather than core.
#   pip install mcp

# --------------------------------------------------------------------------
# Optional, per dispatch channel. Install only the ones you turn on in
# config.toml -- the pipeline degrades to "queue for review" without them.
# --------------------------------------------------------------------------

# dispatch.email -- send applications from your own Gmail account.
#   pip install google-auth google-auth-oauthlib google-api-python-client

# dispatch.ats -- fill Greenhouse/Lever/Ashby forms in a real browser.
# Also doubles as the PDF renderer for resumes, so this one earns its keep.
#   pip install playwright
#   playwright install chromium

# Alternative PDF engine if you don't want Playwright.
#   pip install weasyprint
